node_modules/
.next/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/cache/
*.log
.DS_Store
apps/web/out/
*.tsbuildinfo

# Local env overrides. This is where a key ends up when someone is in a hurry
# (Next reads it automatically), so it must never be committable.
.env.local
.env*.local

# Self-hosted Facebook harvester: login session (cookies!) — never commit
.fb-session/
scripts/fb-harvest.config.json
