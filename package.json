{
  "name": "loppefund",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit && npm run typecheck --workspace @loppefund/web",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test",
    "pipeline": "node packages/pipeline/src/cli.ts",
    "web": "npm run dev --workspace apps/web"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
